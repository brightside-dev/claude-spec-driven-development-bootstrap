# Copy to .env and adjust. .env is gitignored.

# MySQL credentials consumed by docker-compose.yml and the API.
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=app
MYSQL_ROOT_PASSWORD=root

# What the NestJS API connects with (matches the values above).
DATABASE_URL=mysql://app:app@localhost:3306/app
